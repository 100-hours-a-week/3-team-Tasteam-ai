# 학습 전용 의존성 (scripts/train_qlora.py)
# GPU 필수. QLoRA SFT.
#
# 사용: pip install -r requirements.train-llm.txt
# Docker: Dockerfile.train-llm (PyTorch CUDA 베이스)

# LLM / 학습 (버전 고정: transformers/peft/trl 호환성)
# trl 0.12.0 요구: transformers>=4.46.0
transformers>=4.46.0
accelerate>=0.20.0
peft==0.13.2
bitsandbytes>=0.43.0
trl==0.12.0
datasets>=2.18.0
huggingface_hub>=0.20.0

# wandb: report_to=wandb 로깅 + 학습 완료 시 adapter artifact 업로드 (WANDB_RUN_ID 사용 시)
wandb>=0.16.0

# prefect: WANDB_RUN_ID 전달용, train_qlora는 import 안 함. 선택 사항(이미지 절약 시 제거 가능)
# prefect>=2.0.0

# 유틸
python-dotenv>=1.0.0
numpy>=1.24.0
