
**아니요.** `src`의 실제 폴백 문구는 "언급이 적어요"가 아닙니다.

---

## src 요약 파이프라인 폴백

### 1. `CATEGORY_EMPTY_DEFAULT` (리뷰 없음/실패 시)

```24:27:src/summary_pipeline.py
CATEGORY_EMPTY_DEFAULT = {
    "service": {"summary": "아직 충분한 서비스 관련 리뷰 정보가 쌓이지 않았어요.", "bullets": [], "evidence": []},
    "price": {"summary": "아직 충분한 가격 관련 리뷰 정보가 쌓이지 않았어요.", "bullets": [], "evidence": []},
    "food": {"summary": "아직 충분한 음식 관련 리뷰 정보가 쌓이지 않았어요.", "bullets": [], "evidence": []},
}
```

→ 실제로 쓰는 폴백: **"아직 충분한 OOO 관련 리뷰 정보가 쌓이지 않았어요."**

### 2. Price 게이트 (evidence는 있는데 가격 신호 없을 때)

```233:236:src/summary_pipeline.py
    out["price"]["summary"] = "가격 관련 언급이 많지 않아요. 전반적인 만족감이나 구성(양 등) 중심으로만 해석 가능해요."
    out["price"]["bullets"] = [
        "가격을 직접 언급한 리뷰가 많지 않아요.",
```

→ 여기서는 **"가격 관련 언급이 많지 않아요."** / **"가격을 직접 언급한 리뷰가 많지 않아요."**

### 3. LLM 프롬프트의 “언급이 적어요”

```125:src/summary_pipeline.py
- 근거(입력 리뷰)에 없는 내용은 추측하지 말고 "언급이 적어요"처럼 해요체로 표현하라.
```

→ 이건 **예시 표현**으로, “그런 스타일로 써라”는 의미의 **예**일 뿐, 실제 코드에서 고정으로 쓰는 문구는 아님.

---

## 정리

- 실제 **코드 폴백 문구**: `"아직 충분한 ... 리뷰 정보가 쌓이지 않았어요."`, `"가격 관련 언급이 많지 않아요."` 등
- **"언급이 적어요"**: LLM 지시문에서의 예시 표현(스타일 가이드)일 뿐, `src`의 고정 폴백 문구는 아님

---

그러니까 llm 프롬프트의 언급이 적어요는 할루시네이션 방지라고 보는게 맞고, 위의 고정 문구 중 category empty default는 시스템적 폴백이고, Price 게이트 (evidence는 있는데 가격 신호 없을 때) 이거는 llm이 가격이 아닌 요약을 할수 밖에 없는 상황을 원천 방지하는 거라고 보는게 맞지?

---


맞게 이해하신 겁니다.

---

1. **"언급이 적어요" (LLM 프롬프트)**  
   - “근거(입력 리뷰)에 없는 내용은 추측하지 말고 ‘언급이 적어요’처럼 해요체로 표현하라”  
   - 역할: 근거 없는 내용 생성 금지 → **할루시네이션 방지**

2. **CATEGORY_EMPTY_DEFAULT**  
   - `len(payload[cat]) == 0`일 때 적용 (서치 결과 없음, 리뷰 자체가 없음)  
   - 역할: 파이프라인 단계에서 입력이 없을 때 고정 문구로 출력 → **시스템적 폴백**

3. **Price 게이트**  
   - price evidence는 있지만 `_has_price_signal(ev_texts)`가 False인 경우 (가격 관련 표현 없음)  
   - LLM은 “양이 많아요”, “만족해요” 같은 비가격 표현만 보고 가격 요약을 만들 수 있음  
   - 역할: 그런 output을 덮어써서  
     “가격 관련 언급이 많지 않아요…” 로 고정 → **가격이 아닌 evidence로 가격 요약을 하게 되는 상황을 원천 차단**