그 loss 값( **train 1.7759 / eval 1.969** )만 보면 “학습이 망했다” 느낌은 아니야. 오히려 **겉보기엔 정상 범위**일 수 있어.
근데 **지금 너가 겪는 문제(포맷 흔들림/스타일 불안정)** 는 **loss로 잘 안 잡히는 유형**이라서, 저 숫자가 “괜찮다”를 보장하진 않아.

핵심만 정리하면:

## 1) 지금 loss 갭은 크지 않다 → 과적합 신호는 약함

차이 = 1.969 − 1.7759 = **0.1931**.
이 정도면 “train만 잘 맞추고 eval 망함” 같은 전형적 과적합 패턴은 아님.

## 2) 그런데도 문제가 생길 수 있는 이유: **loss는 ‘정답 토큰’만 본다**

너는 사실상 **KD(SFT)로 teacher 출력 텍스트를 따라 쓰게** 만들고 있지?

이때 teacher가 섞여 있으면,

* 어떤 샘플은 4o mini 스타일
* 어떤 샘플은 7B 스타일

이런 **분포 혼합**이 생기는데, 모델은 그냥 “각 샘플의 다음 토큰 예측”만 잘하면 loss가 내려가.
즉, **스타일이 들쭉날쭉해도 loss는 낮게 나올 수 있어.**

특히 JSON 태스크는 더 심해:

* 괄호/따옴표/필드명 1~2개 틀려도 loss 변화는 작을 수 있는데
* 결과물은 **파싱 불가**로 바로 0점이 되잖아.

## 3) 그래서 “loss 정상”인데 “출력은 불안정”이 흔함

정리하면 지금 상태는 딱 이 케이스 가능성이 큼:

* loss: 무난 (teacher 텍스트를 평균적으로 잘 모방)
* output: 불안정 (teacher 스타일/스키마가 섞여서 일관성이 깨짐)

## 4) 이럴 때 확인해야 하는 3가지(바로 효과 큼)

1. **JSON 파싱 성공률** (eval 샘플에서 %로)
2. **스키마 정확도** (필수 키 존재/타입 맞는지)
3. **길이/포맷 drift** (teacher별로 평균 길이, 키 분포가 다른지)

loss보다 이게 훨씬 “진짜 성능”을 말해줘.

## 5) 결론: loss가 저 정도면 “모델 크기(0.5B)가 너무 작아서”라고 단정하긴 어려움

오히려 지금은 우선순위가:

* **teacher 분포를 4o mini로 단일화**
* **JSON 스키마 강제(후처리/constraint/검증 기반 리라이트)**

이 두 개를 먼저 하면, 같은 loss여도 **체감 품질이 확 올라가는** 경우가 많아.

원하면, 네 현재 `val_predictions.json` 같은 샘플 20개만( pred/ref ) 붙여줘.
내가 **“loss는 괜찮은데 왜 output이 망가지는지”**를 케이스별로 딱 갈라서 원인(혼합 teacher vs decoding vs 데이터 포맷 vs tokenizer/템플릿)을 바로 짚어줄게.

---
---

네 해석이 **거의 정확하다.** 👍
조금만 더 정확히 말하면 이거다:

> **“다음 토큰 예측은 평균적으로 잘하지만, 전체 시퀀스를 생성하는 규칙을 안정적으로 학습한 건 아니다.”**

그래서 **loss는 낮은데 output은 흔들리는** 상황이 생긴다.

---

# 1️⃣ 왜 이런 일이 생기냐 (LLM 학습 구조 때문)

LLM은 사실 이렇게 학습한다.

```
P(token_t | token_1 ... token_{t-1})
```

즉 **한 토큰씩 맞추는 문제**다.

하지만 우리가 원하는 것은

```
correct structured output
```

이다.

예:

```
{
  "service": {...},
  "price": {...},
  "food": {...}
}
```

이건 사실

* 구조
* 필드 순서
* 문체
* 길이
* 내용

을 **전체적으로 맞춰야 하는 task**다.

근데 학습은 **토큰 단위**다.

그래서 이런 일이 생긴다.

---

# 2️⃣ teacher가 섞이면 더 심해진다

지금 데이터:

```
4o mini
+
7B
```

이러면 모델이 배우는 것은

```
JSON style A
JSON style B
bullet style C
language mix
```

이다.

그래서 학습은 이렇게 된다.

```
다음 토큰 예측 평균 최적화
```

예:

```
"service":
다음 토큰은

{
  "summary":
또는
{
  "description":
또는
Overall rating
```

이런 식으로 **여러 가능성이 생긴다.**

loss 입장에서는:

```
각 샘플에서는 맞음
```

그래서 loss는 낮다.

하지만 inference에서는

```
랜덤하게 스타일이 섞임
```

---

# 3️⃣ 특히 작은 모델에서 더 심하다

0.5B는 capacity가 작아서

```
teacher distribution을 정확히 분리해서 기억
```

하지 못한다.

그래서 내부적으로

```
평균 스타일
```

을 배운다.

이걸 **mode averaging**이라고 한다.

그래서 결과는

```
이상한 hybrid output
```

이 된다.

예:

```
{
  "service": ...
}

Overall rating: ...
```

같은 것.

---

# 4️⃣ 그래서 loss와 실제 성능이 다를 수 있다

예를 들어:

teacher 1

```
{
  "service": ...
}
```

teacher 2

```
Service Summary:
...
```

모델이 이렇게 출력하면

```
{
  "service":
Service Summary:
```

loss는 크게 증가하지 않는다.

하지만 output은 **완전히 망가진다.**

---

# 5️⃣ 그래서 해결 방법이 두 가지다

## 방법 1 (가장 중요)

**teacher distribution을 하나로 통일**

```
teacher = 4o mini
```

---

## 방법 2

**출력 형식 강제**

예:

```
You must output valid JSON.
Do not include any text outside JSON.
```

---

# 6️⃣ 지금 loss 수치는 오히려 좋은 신호일 수도 있다

```
train 1.7759
eval 1.969
```

이건

```
학습 자체는 안정적
```

이라는 의미다.

즉 문제는

```
optimization 실패 ❌
data distribution 문제 ⭕
```

일 가능성이 높다.

---

# 7️⃣ 정리

네가 말한 것

> 다음 토큰 예측은 잘하는데 추론 진행하면 output이 일정하지 않다

이걸 더 정확히 표현하면

```
token-level objective는 맞지만
sequence-level constraint는 학습되지 않았다
```

이다.

---

💡 참고로 지금 네 상황은 **LLM KD에서 매우 전형적인 실패 패턴**이다.

그래서 하나 말해주면,

지금 파이프라인에서 **딱 한 가지만 바꿔도 성능이 크게 튈 가능성**이 있다.

이건 **teacher 문제가 아니라 “training target 구조” 문제**인데
LLM distillation에서 **진짜 자주 터지는 함정**이다.

원하면 그거 설명해줄게.
(지금 상황에서 꽤 핵심일 가능성이 높다.)

---
---

결론부터 말하면 **`qwen2.5-0.5b-instruct` 자체가 “문제”는 아니다.**
하지만 **지금 같은 태스크에서는 꽤 빡빡한 모델 크기인 건 맞다.**
그래서 **설계가 조금만 흔들리면 바로 성능이 무너지는 구간**이다.

핵심을 정확히 정리해줄게.

---

# 1️⃣ 0.5B 모델 자체는 KD student로 흔히 사용된다

0.5B는 생각보다 **실험용 student로 많이 쓰는 사이즈**다.

대표적인 용도:

* CPU inference
* edge deployment
* latency reduction

예:

| 모델        | 파라미터     |
| --------- | -------- |
| Llama 7B  | 7B       |
| TinyLlama | 1.1B     |
| Qwen2.5   | **0.5B** |

즉

```
teacher 7B
student 0.5B
```

이 구조 자체는 **이상한 실험이 아니다.**

---

# 2️⃣ 다만 0.5B는 “포맷 안정성”이 약하다

특히 이런 태스크에서 약하다.

```
review → structured JSON
```

이건 모델에게 요구하는 능력이 많다.

* instruction following
* structured generation
* hallucination 억제
* style consistency

0.5B는 이걸 동시에 하기가 쉽지 않다.

그래서 자주 나오는 패턴:

```
작동은 한다
하지만 포맷이 흔들린다
```

지금 네 출력 패턴이 정확히 이거다.

---

# 3️⃣ 그런데 지금 로그를 보면 모델 크기 문제보다 더 큰 게 있다

네가 보여준 특징:

* JSON 깨짐
* 영어/중국어 섞임
* hallucination
* 길이 폭주

이건 사실 **0.5B의 전형적인 한계 패턴은 아니다.**

0.5B 한계 패턴은 보통:

```
짧다
단순하다
정보가 부족하다
```

지금은 오히려

```
길다
스타일 섞임
포맷 불안정
```

이다.

그래서 **현재 문제의 1순위 원인은**

```
teacher 데이터 혼합
+
출력 제어 부족
```

일 가능성이 높다.

---

# 4️⃣ 실제로 자주 나오는 오해

많은 사람들이

```
작은 모델 → 성능 낮음
```

이라고 생각하는데 실제 실패 이유는 대부분

```
데이터 분포 문제
```

다.

특히 KD에서는

```
teacher consistency
```

가 훨씬 중요하다.

---

# 5️⃣ 그래서 지금 가장 합리적인 순서

내가 추천하는 순서는 이거다.

### Step 1

teacher 통일

```
teacher = 4o mini only
```

---

### Step 2

JSON schema 강제

```
Return ONLY valid JSON
```

---

### Step 3

0.5B 재학습

---

### Step 4

그래도 부족하면

```
7B → 1.5B → 0.5B
```

---

# 6️⃣ 솔직하게 말하면

지금 태스크 기준으로

```
0.5B
```

는 **가능하지만 margin이 작다.**

그래서 보통 sweet spot은

```
1B ~ 3B
```

이다.

하지만 **포맷 잘 잡으면 0.5B도 충분히 돌아간다.**

---

# 7️⃣ 한 줄 결론

```
student = qwen2.5-0.5b-instruct
```

👉 **문제 없음**

하지만

```
teacher 혼합
+
schema 제어 없음
```

이 상태에서는 **어떤 모델을 써도 불안정해질 가능성이 높다.**

---

💡 하나만 더 말하면

지금 네 실험 로그를 보면 **0.5B가 “딱 하나의 구조”를 못 배우고 있는 흔적**이 보인다.

이거 하나만 해결하면 **ROUGE / judge score가 꽤 크게 튈 가능성**이 있다.
LLM KD에서 **진짜 자주 터지는 문제**다.
