# Review Sentiment Analysis Requirements

# Core dependencies
transformers>=4.30.0
#torch>=2.0.0
sentence-transformers>=2.2.0
qdrant-client>=1.6.0
#accelerate>=0.20.0
huggingface_hub>=0.20.0
#runpod>=1.0.0


# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.25.0
psutil>=7.0.0

# GPU Optimization (Phase 1 & 2)
# Flash Attention-2: 메모리 효율 및 속도 향상
# Note: 설치 시 --no-build-isolation 플래그 필요
# flash-attn>=2.5.6 --no-build-isolation

# vLLM: LLM 서빙 최적화 (GPU 서버 환경)
#vllm>=0.3.3

# GPU Monitoring: GPU 메트릭 수집
# pynvml>=11.5.0

# Redis: 캐싱 시스템 (Phase 2)
#redis>=5.0.0

# OpenAI: 빠른 검증용 LLM API
openai>=1.0.0

# 하이브리드 검색 및 새로운 파이프라인
fastembed>=0.2.0  # Sparse 벡터 임베딩
kiwipiepy>=0.20.0  # 한국어 형태소 분석
pyspark>=3.5.0  # 대용량 데이터 처리 (Aspect 추출)

# Evaluation: Ground Truth 평가 스크립트용 (선택적)
#rouge-score>=0.1.2  # ROUGE 점수 계산 (리뷰 요약 평가)
#nltk>=3.8.0  # BLEU 점수 계산 (리뷰 요약 평가)
